# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name = "{{ project_name }}",
    version = "0.0.1",
    url = "",
    license = "",
    description = "",
    author = '',
    packages = find_packages(),
    package_dir = {'': '.'},
    install_requires = [],
)
